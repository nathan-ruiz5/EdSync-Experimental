import SwiftUI

struct AppFooter: View {
    var body: some View {
        Text("Prototype -- features may be limited")
            .font(.caption2)
            .multilineTextAlignment(.center)
            .padding()
            .foregroundStyle(.gray)
    }
}